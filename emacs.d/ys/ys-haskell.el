;;; ~/.emacs.d/ysgard/ysgard-haskell.el --- Summary

;; Haskell Setup

;;; Commentary:

;; Haskell-specific setup (ghc-mod, hlint, etc...) gets setup here

;;; Code:

;; intero
(add-hook 'haskell-mode-hook 'intero-mode)

(provide 'ysgard-haskell)
;;; ysgard-haskell.el ends here
